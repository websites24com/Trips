doctype html
html
    head
        block head
            meta(charset='UTF-8')
            meta(name='viewport' content='width=device-width, initial-scale=1.0')
            // Static files comes from public folder we define the path in app.js 
            link(rel='stylesheet' href='/css/style.css')
            
            link(rel='short icon' type='image/png' href='/img/favicon.png')
            link(rel='stylesheet' href='https://fonts.googleapis.com/css?family=Lato:300,300i,700')
            title Natours | #{title}
            script(src="https://js.stripe.com/clover/stripe.js" async)
            
            
    body
        // HEADER
        include _header

        // CONTENT
        block content
            h1 This is a placeholder heading
        // FOOTER
        include _footer

        // SCRIPTS

       
        script(src="/dist/js/main.js")
       
      



